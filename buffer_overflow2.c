#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(void) {
    int a = 64;
    
    int b = a + 1;
    int c = a + 2;
    int d = a + 3;
    int e = a + 4;

    int f = 128;

	char buf[a];
	memset(buf, 0, a);
    buf[f] = 'l'
}